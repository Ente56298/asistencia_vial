{
  "tests": [
    {
      "name": "MCP Configuration Test",
      "cases": [
        {
          "name": "Should have mcpServers property",
          "assertion": "mcpServers in data"
        },
        {
          "name": "Should have firebase server configuration",
          "assertion": "'firebase' in data.mcpServers"
        },
        {
          "name": "Firebase server command should be 'npx'",
          "assertion": "data.mcpServers.firebase.command == 'npx'"
        },
        {
          "name": "Firebase server should have 5 arguments",
          "assertion": "len(data.mcpServers.firebase.args) == 5"
        },
        {
          "name": "Firebase server arguments should be correct",
          "assertion": "data.mcpServers.firebase.args[0] == '-y' and data.mcpServers.firebase.args[1] == 'firebase-tools@latest' and data.mcpServers.firebase.args[2] == 'experimental:mcp' and data.mcpServers.firebase.args[3] == '--only' and data.mcpServers.firebase.args[4] == 'auth,firestore'"
        }
      ]
    }
  ]
}
